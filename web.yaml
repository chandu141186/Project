- name : web
  hosts: web
  become: yes
  tasks:
   - name: Install Nginx
     ansible.builtin.package:
         name: Nginx
         state: present

   - name: Start and Enable Mongodb service
     ansible.builtin.service:
         name: Nginx
         state: started
         enabled: yes
